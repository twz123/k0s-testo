{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    ":configMigration",
    ":gitSignOff"
  ],
  "packageRules": [
    {
      "description": "Update etcd versions across the codebase",
      "enabled": true,
      "groupName": "etcd dependencies",
      "groupSlug": "etcd-all",
      "matchManagers": [
        "gomod",
        "custom"
      ],
      "matchPackageNames": [
        "go.etcd.io/etcd/**",
        "etcd-io/etcd"
      ]
    },
    {
      "description": "Update kine versions across the codebase",
      "enabled": true,
      "matchManagers": [
        "custom"
      ],
      "matchPackageNames": [
        "k3s-io/kine"
      ]
    },
    {
      "description": "Disable updates for everything else.",
      "enabled": false
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Update etcd versions across the codebase",
      "managerFilePatterns": [
        "^embedded-bins/Makefile\\.variables$"
      ],
      "matchStrings": [
        "etcd_version = (?<currentValue>v?[\\d\\.]+)"
      ],
      "depNameTemplate": "etcd-io/etcd",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    },
    {
      "customType": "regex",
      "description": "Update kine versions across the codebase",
      "fileMatch": [
        "(^|/)[Mm]akefile(\\.[^/]+)?$",
        "\\.(go|mk)$"
      ],
      "matchStrings": [
        "kine_version = (?<currentValue>v?[\\d\\.]+)",
        "\\bkine/blob/(?<currentValue>v?[\\d\\.]+)\\b"
      ],
      "depNameTemplate": "k3s-io/kine",
      "datasourceTemplate": "github-releases",
      "versioningTemplate": "semver"
    }
  ]
}
